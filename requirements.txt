# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads
# the committed data/ledger/*.jsonl directly, so it only needs streamlit. The
# CLI/package under brief_calibration/ has its own deps in pyproject.toml.
streamlit>=1.40
